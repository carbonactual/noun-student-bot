const {createEvent,studentBrief}=require('../lib/abba');
const SECRET=process.env.WEBHOOK_SECRET;
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(SECRET&&req.headers['x-webhook-secret']!==SECRET)return res.status(401).json({error:'Unauthorized'});try{const action=req.body?.action;if(action==='event')return res.status(200).json({ok:true,event:await createEvent(req.body)});if(action==='student_brief'){if(!req.body?.phone)return res.status(400).json({error:'phone required'});return res.status(200).json({ok:true,brief:await studentBrief(String(req.body.phone),req.body.query||'')});}return res.status(400).json({error:'Unknown action'});}catch(e){console.error(e);return res.status(500).json({error:'ABBA intelligence operation failed'});}};
