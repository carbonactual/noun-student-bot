const {searchServices,upcomingActivities}=require('../lib/student-services');
const SECRET=process.env.WEBHOOK_SECRET;
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(SECRET&&req.headers['x-webhook-secret']!==SECRET)return res.status(401).json({error:'Unauthorized'});try{const q=String(req.body?.q||'').trim();const category=String(req.body?.category||'').trim()||null;const [services,activities]=await Promise.all([searchServices(q,category),upcomingActivities(20)]);return res.status(200).json({ok:true,services,activities,notice:'Listings are informational. Verification status, provider identity, price and NOUN endorsement must be checked before a student pays or relies on a service.'});}catch(e){console.error(e);return res.status(500).json({error:'Service discovery failed'});}};
